# Taskovia C1 v1.2 implementation plan

**Normative baseline:** v1.2 amends v1.1; unchanged v1.1 requirements remain mandatory.

## Global constraints

- P1 is COMPLETE. P2 v1.1 migration is unapplied and must not be applied unchanged.
- Cloud DEV is the only database and only under explicit authorization; no Local DB, Production, or real VQH import without a real-data packet.
- No generic ETL, runtime upload feature, browser preview, C2/C3, P3 financial activation, or second competing P2 migration.

## P2.0 — reconcile superseded WIP

Before any P2 write, apply the transition disposition: KEEP source/provenance/decimal concepts; SIMPLIFY source service/routes into manifest/import-run paths; DEFER file/storage/preview/SourceWizard runtime features; REMOVE BEFORE APPLY the current `20260912062227_taskovia_c1_sources_files.sql` and obsolete fixtures by either rewriting the unapplied migration or removing it then generating one reviewed v1.2 migration. Do not leave both architectures active. Commit this reconciliation separately.

## P2.1 — controlled-import contracts

Create strict shared schemas/interfaces for source/version, provenance unit, source figure, review issue, manifest, manifest digest, import run, adapter output, and idempotency. Test decimal strings, unresolved/pending mapping, manifest mismatch, duplicate identity, replay/mismatch, no fake master data, and non-posting behavior. Commit only after focused tests pass.

## P2.2 — first workbook-family adapter

For an explicitly supplied workbook, analyze structure; generate a human-reviewable manifest; obtain review; freeze manifest digest; execute a focused adapter/import command; validate deterministic result. Keep adapter analysis separate from source persistence, never commit real values, and reject unsupported structure before mutation. Track B may analyze files and prepare manifests without DB writes only after Track A freezes the manifest contract.

## P2.3 — persistence and synthetic acceptance

Create/review one forward migration for source/import-run/manifest persistence, tenant/company RLS, receipts/events, provenance, immutable source history, and non-posting behavior. Add self-contained rollback-safe synthetic fixtures for isolation, replay, manifest mismatch, provenance, and zero financial activation. Cloud migration, fixtures, and generated types each need separate explicit authorization.

## P2.4 — controlled real-data onboarding

After synthetic P2 acceptance, require a separate real-data packet naming target company/environment, source files/digests, adapter version, reviewed manifest digest, expected counts, unresolved records, replay behavior, zero-financial-effect checks, stop conditions, and validation/reporting commands. This plan does not authorize that operation.

## Downstream phases

P3 consumes imported P2 provenance rather than runtime upload lifecycle, but retains all inherited financial requirements. P4 is unchanged except wording that refers to deferred file mechanics. P5 retains reporting/source display but defers mandatory upload/preview UX. P6 verifies controlled-import provenance, isolation, non-posting behavior, financial correctness, and reporting truthfulness.
