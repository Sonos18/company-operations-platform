# VQH Stage 01 Phase A Acceptance Corrections Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task in the current session. Inline execution is the user-selected mode; do not dispatch subagents. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Status:** APPROVED — reviewed by Sơn on 2026-08-30
> **Implementation authorization:** AUTHORIZED BY APPROVED FIX PACKET AFTER `READY` OR `READY_WITH_NON_MATERIAL_DRIFT` PREFLIGHT
> **Plan date:** 2026-08-30
> **Reviewed implementation:** `feat/vqh-stage-01-foundation@78bf5151f3c46527f53350edd18a79f7f1778677`
> **Corrective design source commit:** `e228b739ffffb2e1a5b12fba1e5460fa4da86abd`; approval metadata is recorded with this plan

**Goal:** Correct the four verified VQH Stage 01 Phase A runtime defects and replace the false advisory-lock acceptance claim with real one-winner optimistic-concurrency evidence, without expanding Phase A or reopening approved business decisions.

**Architecture:** Preserve the existing Opportunity aggregate, reusable Workflow Core, explicit public RPCs, immutable bound definition, user-scoped Supabase request path, RLS, append-only events/audit, and event-first 01.1 completion order. Deliver database changes in one forward-only corrective migration, update the strict TypeScript/HTTP contracts, and verify nine fixed public-RPC races through separate Cloud DEV PostgreSQL sessions.

**Tech Stack:** pnpm 10.29.3, Node.js 24.x, Nuxt 4.3.1, Vue 3.5.28, TypeScript 5.9.3, Supabase JS 2.112.x, Supabase CLI 2.114.0, canonical Supabase Cloud DEV, PostgreSQL/RLS, Zod 4.x, Vitest 4.1.9, Playwright 1.61.1.

**Spec:** [`docs/superpowers/specs/2026-08-30-vqh-stage-01-acceptance-corrections-design.md`](../specs/2026-08-30-vqh-stage-01-acceptance-corrections-design.md)

## Global Constraints

- Continue on `feat/vqh-stage-01-foundation`; do not create a branch or worktree.
- Before implementation, require an approved Fix Packet with `reviewed_head_sha: 78bf5151f3c46527f53350edd18a79f7f1778677`, the five findings, the approved design and this plan as declared documentation prerequisites, and explicit side-effect authorization.
- Do not start Task 1 unless technical preflight returns `READY` or `READY_WITH_NON_MATERIAL_DRIFT`. Stop on `PACKET_STALE` or `BLOCKED`.
- Follow TDD for every behavior change: add the focused failing test, record the expected failure, implement the minimum correction, rerun the focused test, then commit.
- Use one new forward-only migration generated by `pnpm exec supabase migration new stage01_acceptance_corrections`. Never edit migrations `20260829120100` through `20260829120800`.
- Never hand-edit `shared/types/database.types.ts`; regenerate it from the guarded canonical Cloud DEV project after the corrective migration is applied.
- Do not run Docker, local Supabase, `db:local:*`, `verify:backend:local`, `supabase start`, `supabase db reset`, migration repair, remote reset, seed, dashboard/Table Editor schema changes, or arbitrary linked SQL.
- Cloud DEV migration push and fixed-fixture mutations require `cloud_dev_mutating: true` in the approved Fix Packet. If absent, stop before Task 2 Step 6 and do not run any later Cloud DEV mutation.
- Production mutation, deployment, merge, and force-push remain unauthorized.
- Do not resolve or infer `BDG-TAX-01`, `BDG-EVAL-01`, `BDG-AUTH-01`, or `BDG-HIER-01`.
- Do not publish concrete VQH taxonomy values, evaluation criteria, authority rules, operational roles, Project, Project Manager assignment, canonical parent Stage 01 runtime, Stage 02 runtime, or a Stage 01 UI migration.
- Keep configurable taxonomy codes as non-empty strings in TypeScript. PostgreSQL validates them against the immutable definition bound to the Opportunity workflow instance.
- Preserve public RPC names and SQL signatures. Do not introduce a generic RPC proxy or a new stable public error code.
- Preserve `SECURITY INVOKER` public wrapper → private `SECURITY DEFINER SET search_path = ''` implementation, schema-qualified objects, live membership/permission checks, exact grants, and the prohibition on normal-path `service_role`.
- Preserve aggregate lock-before-version-check ordering and `VERSION_CONFLICT` for stale versions.
- Preserve every existing completion baseline; schema version 1 applies only to new 01.1 completions.
- Never log a Management API response body, token, JWT claims, contact value, or other secret/PII. The concurrency harness may expose only an allowlisted parsed error code.
- Fixed Cloud DEV test fixtures must be deterministic, namespaced, and cleaned in `finally`; cleanup failure takes precedence over the race result and must be disclosed.

---

## File Map

### Approval and documentation

- Modify `docs/superpowers/specs/2026-08-30-vqh-stage-01-acceptance-corrections-design.md` — record written approval.
- Use an approved Fix Packet under `docs/ai-workflow/packets/` as the authoritative implementation authorization; its exact path is supplied before execution and is not invented by this plan.

### Shared and HTTP contracts

- Modify `shared/schemas/workflow.ts` — require non-empty revalidation evidence.
- Modify `shared/schemas/opportunities.ts` — accept optional restore evidence while leaving its semantic requirement to the bound-definition database boundary.
- Modify `app/repositories/contracts.ts` only if inferred type propagation exposes a mismatch; do not duplicate schema shapes.
- Modify `app/repositories/http/http-workflow-repository.ts` and `app/repositories/http/http-opportunity-repository.ts` only as required to forward parsed inputs unchanged.
- Modify `server/features/workflow/workflow.routes.ts`, `server/features/workflow/workflow.service.ts`, `server/features/workflow/workflow.repository.ts`, `server/features/opportunities/opportunity.routes.ts`, `server/features/opportunities/opportunity.service.ts`, and `server/features/opportunities/opportunity.repository.ts` only if tests prove evidence is dropped.
- Modify focused unit tests under `tests/unit/shared/`, `tests/unit/server/`, and `tests/unit/repositories/`.

### Corrective database migration and SQL acceptance tests

- Create `supabase/migrations/YYYYMMDDHHMMSS_stage01_acceptance_corrections.sql` with the timestamp assigned by `supabase migration new` — taxonomy helper/validation, explicit baseline schema version 1, required revalidation evidence, current invalidation state, duplicate-merged restore rule, and replacement command implementations.
- Modify `supabase/tests/database/stage01_schema.test.sql`.
- Modify `supabase/tests/database/stage01_definition.test.sql`.
- Modify `supabase/tests/database/stage01_bootstrap.test.sql`.
- Modify `supabase/tests/database/stage01_security.test.sql`.
- Modify `supabase/tests/database/stage01_history.test.sql`.
- Modify `supabase/tests/database/stage01_commands.test.sql`.
- Modify `supabase/tests/database/stage01_flows.test.sql`.

### Real concurrency evidence

- Modify `scripts/run-stage01-cloud-dev-concurrency.mjs` — fixed scenario inventory, sanitized Management API result parsing, exact one-success/one-`VERSION_CONFLICT` assertion, and cleanup precedence.
- Modify `scripts/run-supabase-dev.mjs` — remove obsolete single-scenario concurrency modes; keep only fixed allowlisted behavior needed by current package scripts.
- Modify `tests/unit/config/stage01-cloud-dev-concurrency.spec.ts` and `tests/unit/config/supabase-cloud-dev-runner.spec.ts`.
- Delete the five obsolete advisory-lock files `supabase/tests/database/stage01_concurrency_{setup,actor_a,actor_b,assert,cleanup}.sql` after the real matrix is complete.
- Create `setup.sql`, `actor-a.sql`, `actor-b.sql`, `assert.sql`, and `cleanup.sql` inside each of the nine exact scenario directories listed next.
- Required scenario directories: `opportunity-update`, `primary-contact`, `primary-referrer`, `duplicate-resolution`, `contact-update`, `intake-complete`, `node-execution`, `final-decision`, and `reactivation`.

### Generated and regression artifacts

- Regenerate `shared/types/database.types.ts` through `pnpm db:dev:types`.
- Modify only tests or types directly required by the regenerated contract; no unrelated cleanup.

---

### Task 0: Re-preflight the approved fix round

**Files:**

- Read the approved Fix Packet supplied for this correction.
- Read `AGENTS.md`, the approved design, and this plan.
- Do not modify runtime files in this task.

- [ ] **Step 1: Fetch and capture the SHA model**

Run:

```powershell
git fetch origin --prune
git rev-parse origin/docs/vqh-stage-01-technical-spec
git rev-parse origin/feat/vqh-stage-01-foundation
git rev-parse HEAD
git merge-base origin/docs/vqh-stage-01-technical-spec HEAD
git rev-list --left-right --count origin/feat/vqh-stage-01-foundation...HEAD
```

Expected: the reviewed remote implementation remains `78bf5151f3c46527f53350edd18a79f7f1778677`; local-only commits before runtime work are exactly the approved corrective design and execution plan declared by the Fix Packet.

- [ ] **Step 2: Inspect checkout ownership and safety**

Run:

```powershell
git status --short --branch
git branch -vv
git worktree list --porcelain
git diff --check
git diff --name-status 78bf5151f3c46527f53350edd18a79f7f1778677..HEAD
```

Expected: current branch is `feat/vqh-stage-01-foundation`, checkout is clean, no conflicting branch ownership exists, and the pre-implementation delta is documentation-only.

- [ ] **Step 3: Verify runtime, dependency, command, and Cloud DEV guards without mutation**

Run:

```powershell
node --version
pnpm --version
pnpm exec supabase --version
pnpm db:dev:target
pnpm db:dev:auth-check
pnpm db:dev:status
pnpm db:dev:dry-run
```

Inspect `package.json`, `pnpm-lock.yaml`, `scripts/assert-cloud-dev-target.mjs`, and the Fix Packet side-effect matrix. `dry-run` must show no undeclared migration. Do not run `db:dev:push`.

- [ ] **Step 4: Return exactly one preflight status**

Return `READY`, `READY_WITH_NON_MATERIAL_DRIFT`, `PACKET_STALE`, or `BLOCKED` with `analysis_base_sha`, `remote_base_sha`, `execution_base_sha`, remote implementation SHA, branch ownership, validation availability, side-effect authorization, and any drift. Continue only for the first two statuses.

---

### Task 1: Preserve revalidation and restore evidence through strict application contracts

**Files:**

- Modify `shared/schemas/workflow.ts`.
- Modify `shared/schemas/opportunities.ts`.
- Modify `tests/unit/shared/stage01-schemas.spec.ts`.
- Modify `tests/unit/server/workflow-routes.spec.ts`.
- Modify `tests/unit/server/workflow.repository.spec.ts`.
- Modify `tests/unit/repositories/http-workflow-repository.spec.ts`.
- Modify `tests/unit/server/opportunity-routes.spec.ts`.
- Modify `tests/unit/server/opportunity.repository.spec.ts`.
- Modify `tests/unit/repositories/http-opportunity-repository.spec.ts`.
- Modify production adapter files only if a failing test identifies an actual drop point.

**Contract:**

```ts
export const revalidateWorkflowNodeInputSchema = z.object({
  reason: meaningfulTextSchema,
  evidence: z.array(z.unknown()).min(1),
  expectedExecutionVersion: versionSchema,
}).strict()

export const restoreOpportunityInputSchema = z.object({
  reason: meaningfulTextSchema,
  evidence: z.array(z.unknown()).optional(),
  expectedOpportunityVersion: versionSchema,
}).strict()
```

- [ ] **Step 1: Add failing shared-schema tests**

Assert that revalidation accepts `evidence: ['baseline:2']`, rejects missing evidence, rejects `evidence: []`, and remains strict. Assert that restore accepts no evidence for ordinary invalidation, accepts a JSON evidence array, and rejects an unexpected key.

Run:

```powershell
pnpm vitest run tests/unit/shared/stage01-schemas.spec.ts
```

Expected red: current revalidation schema rejects supplied evidence and accepts a body without it; current restore schema rejects supplied evidence.

- [ ] **Step 2: Add failing HTTP and server forwarding tests**

Use a nested evidence value so a shallow boolean-only assertion cannot pass accidentally:

```ts
const evidence = [{ kind: 'baseline_ref', ref: 'baseline:2' }]
```

Assert the Nitro route, service/repository RPC args, and frontend HTTP request body preserve the exact array for both commands. Also assert the HTTP revalidation route returns validation failure when evidence is missing or empty.

Run:

```powershell
pnpm vitest run tests/unit/server/workflow-routes.spec.ts tests/unit/server/workflow.repository.spec.ts tests/unit/repositories/http-workflow-repository.spec.ts tests/unit/server/opportunity-routes.spec.ts tests/unit/server/opportunity.repository.spec.ts tests/unit/repositories/http-opportunity-repository.spec.ts
```

Expected red: the current strict schemas block the new evidence payloads.

- [ ] **Step 3: Implement the minimum shared contract change**

Add the two schema fields above. Prefer inferred input types so route, service, repository, and app contracts update without duplicate interfaces. Change an adapter only if Step 2 proves it reconstructs and drops fields.

- [ ] **Step 4: Run the focused green suite and typecheck**

```powershell
pnpm vitest run tests/unit/shared/stage01-schemas.spec.ts tests/unit/server/workflow-routes.spec.ts tests/unit/server/workflow.repository.spec.ts tests/unit/repositories/http-workflow-repository.spec.ts tests/unit/server/opportunity-routes.spec.ts tests/unit/server/opportunity.repository.spec.ts tests/unit/repositories/http-opportunity-repository.spec.ts
pnpm typecheck
git diff --check
```

- [ ] **Step 5: Commit the contract slice**

```powershell
git add shared/schemas tests/unit app/repositories server/features
git commit -m "fix: preserve stage 01 correction evidence"
```

Review the staged paths before committing; exclude unchanged adapter directories.

---

### Task 2: Add database acceptance tests that expose all four runtime defects

**Files:**

- Modify the seven allowlisted Stage 01 SQL test files listed in the File Map.
- Do not create or edit a migration until the focused red evidence is observed.

**Taxonomy mapping under test:**

| Field | Bound snapshot key |
| --- | --- |
| `customerTypeCode` | `customer_type` |
| `relationshipCode` | `contact_relationship` |
| `scopeCode` | `scope` |
| `primaryLeadSourceCode` | `lead_source` |
| `referrerTypeCode` | `referrer_type` |
| `engagementStatusCode` | `engagement_status` |
| `invalidReasonCode` | `invalid_reason` |
| `budgetStatusCode` | `budget_status` |
| `timelineStatusCode` | `timeline_status` |
| `priorityCode` | `priority` |
| `channelCode` | `intake_channel` |
| blocker `categoryCode` | `blocker_category` |

- [ ] **Step 1: Extend synthetic definition fixtures without choosing VQH business values**

Add technical test-only entries for every mapped key. Include a Lead Source with `behavior.requiresReferrer = true`, an invalid-reason entry with `semanticKey = duplicate_merged`, and one ordinary invalidation semantic. Keep `begin;` as the first statement and `rollback;` as the final statement in every rollback-safe SQL file, and use deterministic UUID namespaces.

- [ ] **Step 2: Add failing taxonomy and completion tests**

Cover bootstrap and every command boundary that supplies a mapped code. Each unknown code must return `INVALID_COMMAND_INPUT` and leave versions, current rows, event count, and audit count unchanged. Add a defense-in-depth test that injects an unknown persisted Lead Source through privileged fixture setup and proves `complete_stage01_intake` fails rather than deriving `requiresReferrer = false`.

- [ ] **Step 3: Add failing explicit-baseline and immutability tests**

After a valid 01.1 completion, assert `snapshot.schemaVersion = 1` and all approved groups exist: Opportunity subset, Primary Contact, usable Contact Method references, active Scopes, Primary Referrer, Intake Record references, Intake Owner assignment, explicit gates, and completion actor/time/versions. Assert no Contact Method value is copied. Then update the Contact Method through `public.update_contact_method` and prove the stored snapshot and SHA-256 hash remain unchanged and still show `isUsableAtCompletion: true`.

- [ ] **Step 4: Add failing revalidation and duplicate-restore database tests**

Assert `public.revalidate_workflow_node` rejects missing evidence, non-array evidence, and an empty array, while recording a non-empty array in the event/audit on success. Assert `duplicate_merged` restore fails without evidence and with an empty array; succeeds with explicit correction/separation evidence; clears canonical and current invalidation fields atomically; increments version once; and writes one audit transition. Assert ordinary restore still succeeds without evidence.

- [ ] **Step 5: Add schema and security assertions**

Assert the five current invalidation columns, nullability/consistency constraint, private taxonomy helper signature, function security attributes, revocations, and unchanged authenticated direct-DML prohibition.

- [ ] **Step 6: Run the rollback-safe red database suite**

```powershell
pnpm db:dev:target
pnpm db:dev:stage01:test
```

Expected red: the current Cloud DEV schema lacks the invalidation columns/helper, accepts at least one unknown code, emits an incomplete baseline, allows missing revalidation evidence, or restores duplicate semantics without evidence. Record the first failing assertion, then continue only after confirming it is one of these expected defects. The SQL rolls back all fixtures.

Do not commit a tests-only state that makes the branch's required Stage 01 suite permanently red; proceed directly to Task 3.

---

### Task 3: Implement one forward-only atomic database correction

**Files:**

- Create `supabase/migrations/YYYYMMDDHHMMSS_stage01_acceptance_corrections.sql`, where the exact timestamped filename is the direct output of Task 3 Step 1.
- Complete the SQL test changes started in Task 2.

- [ ] **Step 1: Generate the migration through the pinned CLI**

```powershell
pnpm exec supabase migration new stage01_acceptance_corrections
git status --short
```

Capture the exact generated path and use it for every following step. Do not rename it or fabricate a timestamp.

- [ ] **Step 2: Add the bound-definition taxonomy helper**

Implement:

```sql
private.stage01_taxonomy_entry(
  definition jsonb,
  taxonomy_key text,
  supplied_code text
) returns jsonb
```

It must fail with `INVALID_COMMAND_INPUT` when the key is absent/not an array, the code is blank/absent, or the match count is not exactly one. It returns the snapshotted entry, including optional `semanticKey` and `behavior`. Revoke execution from `public`, `anon`, and `authenticated`.

- [ ] **Step 3: Validate every mapped code at the correct command boundary**

Replace `private.execute_stage01_opportunity_command` and `private.execute_stage01_workflow_command` with `create or replace function` while preserving signatures, permissions, scoping, lock order, and response JSON. Bootstrap validates against the definition selected inside its transaction; subsequent commands join the existing Opportunity workflow instance to its bound snapshot. Revalidate persisted gate-related codes during 01.1 completion.

- [ ] **Step 4: Build deterministic baseline schema version 1**

Replace the full-row dump with the exact explicit payload in the approved design. Capture one `completion_at`, order arrays by timestamp then ID, emit consistent `null`/empty-array values, exclude Contact Method values, retain event-first insertion, and hash the complete JSONB snapshot. Add no update path for prior baselines.

- [ ] **Step 5: Require revalidation evidence in PostgreSQL**

For `revalidate_node`, include `evidence` in allowed and required keys; require JSON array and length greater than zero; remove `coalesce(..., '[]')`; and write supplied evidence into the node event and audit context.

- [ ] **Step 6: Add and guard current invalidation state**

Add:

```sql
current_invalid_reason_code text null,
current_invalid_reason_semantic_key text null,
current_invalidation_reason text null,
invalidated_by uuid null references auth.users (id) on delete restrict,
invalidated_at timestamptz null
```

Before enforcing a consistency constraint, inspect invalid Opportunities and backfill only from an unambiguous latest invalidation/same-need duplicate-resolution audit. Abort with affected IDs if any row cannot be reconstructed. Valid rows must have all fields null; new invalid rows must have a complete tuple. For the same-need duplicate path, use the internal reserved code `system_same_need_duplicate` plus semantic key `duplicate_merged`; do not present that reserved code as a company taxonomy value.

- [ ] **Step 7: Enforce duplicate-merged restore semantics atomically**

`invalidate_opportunity` resolves `invalidReasonCode` from the bound snapshot and captures `semanticKey`; `duplicate_merged` requires a valid same-company canonical Opportunity. Same-need duplicate resolution records the technical `duplicate_merged` semantic. Restore requires a non-empty evidence array only for that semantic, then clears canonical/current invalidation state, marks valid, increments the Opportunity version, and writes evidence to audit in the same transaction.

- [ ] **Step 8: Inspect locally and run guarded pre-push checks**

```powershell
git diff --check
git diff --stat
pnpm vitest run tests/unit/shared/stage01-schemas.spec.ts tests/unit/server/workflow-routes.spec.ts tests/unit/server/workflow.repository.spec.ts tests/unit/repositories/http-workflow-repository.spec.ts tests/unit/server/opportunity-routes.spec.ts tests/unit/server/opportunity.repository.spec.ts tests/unit/repositories/http-opportunity-repository.spec.ts
pnpm db:dev:target
pnpm db:dev:auth-check
pnpm db:dev:status
pnpm db:dev:dry-run
```

Expected: dry-run lists exactly the one generated corrective migration. Stop if it lists anything else.

- [ ] **Step 9: Apply the authorized migration once and run the green SQL suite**

This step is allowed only when the approved Fix Packet sets `cloud_dev_mutating: true`.

```powershell
pnpm db:dev:push
pnpm db:dev:status
pnpm db:dev:stage01:test
```

Expected: migration status is aligned and all rollback-safe Stage 01 SQL files pass. If push succeeds but tests fail, do not edit the applied migration; generate a new forward corrective migration only after diagnosis and packet-scope confirmation.

- [ ] **Step 10: Commit the database correction**

```powershell
git add supabase/migrations supabase/tests/database
git diff --cached --check
git commit -m "fix: close stage 01 acceptance gaps"
```

---

### Task 4: Replace advisory-lock evidence with nine real public-RPC races

**Files:**

- Modify the runner and runner unit tests listed in the File Map.
- Create the nine fixed scenario directories and delete the obsolete advisory-lock files only after all replacements exist.

**Fixed scenario/RPC matrix:**

| Scenario | Both actors call | Owning expected version |
| --- | --- | --- |
| `opportunity-update` | `public.update_opportunity_current_data` | `expectedOpportunityVersion` |
| `primary-contact` | `public.set_opportunity_primary_contact` | `expectedOpportunityVersion` |
| `primary-referrer` | `public.set_opportunity_primary_referrer` | `expectedOpportunityVersion` |
| `duplicate-resolution` | `public.resolve_opportunity_duplicate` | `expectedOpportunityVersion` |
| `contact-update` | `public.update_contact` | `expectedContactVersion` |
| `intake-complete` | `public.complete_stage01_intake` | Opportunity + execution versions |
| `node-execution` | `public.assign_workflow_node` | `expectedExecutionVersion` |
| `final-decision` | `public.record_stage01_final_decision` | `expectedCycleVersion` |
| `reactivation` | `public.reactivate_stage01` | Opportunity + execution + cycle versions |

- [ ] **Step 1: Add failing runner contract tests**

Assert the fixed inventory is exactly the nine names above; setup precedes actors; actors launch concurrently; one actor must return success; the only accepted loser is a sanitized parsed `VERSION_CONFLICT`; two successes, two conflicts, network failure, malformed response, or another database error all fail; assertion runs only after a valid race; cleanup always runs and cleanup failure takes precedence. Assert raw response bodies and tokens never reach error messages or logs.

Run:

```powershell
pnpm vitest run tests/unit/config/stage01-cloud-dev-concurrency.spec.ts tests/unit/config/supabase-cloud-dev-runner.spec.ts
```

Expected red: the current harness accepts any rejected actor and has only one advisory-lock scenario.

- [ ] **Step 2: Implement sanitized Management API outcomes and fixed inventory**

Return an internal result such as:

```js
{ ok: true }
{ ok: false, code: 'VERSION_CONFLICT' }
```

Parse the database error privately, allowlist only `VERSION_CONFLICT`, and replace all unexpected body content with a generic status/scenario error. The runner accepts no CLI arguments, paths, identifiers, project refs, SQL, or fixture inputs.

- [ ] **Step 3: Create deterministic SQL for each scenario**

Every actor file must contain the fixed marker, `begin`, fixed authenticated local claims, the same public RPC/aggregate IDs/expected versions, and `commit` only after RPC success. Each assertion proves one version increment, one winning current mutation, no losing event/audit/history/baseline/cycle residue, and preserved earlier immutable history. Cleanup removes every namespaced row in dependency order and is safe to rerun.

- [ ] **Step 4: Prove the files contain business RPCs rather than locks**

Add unit assertions that both actor files for every scenario contain the exact public RPC from the matrix and the applicable aggregate-version key, and contain neither `pg_advisory` nor `pg_sleep`. Verify the old five advisory files are absent.

- [ ] **Step 5: Run unit tests, then the authorized Cloud DEV matrix**

```powershell
pnpm vitest run tests/unit/config/stage01-cloud-dev-concurrency.spec.ts tests/unit/config/supabase-cloud-dev-runner.spec.ts
pnpm db:dev:target
pnpm db:dev:stage01:concurrency
```

Expected: all nine scenarios report exactly one committed winner and one exact `VERSION_CONFLICT`, all final-state assertions pass, and final cleanup succeeds. This fixed-fixture mutation requires `cloud_dev_mutating: true` in the Fix Packet.

- [ ] **Step 6: Commit the concurrency evidence**

```powershell
git add scripts tests/unit/config supabase/tests/database
git diff --cached --check
git commit -m "test: prove stage 01 optimistic concurrency"
```

---

### Task 5: Regenerate database types and run the full corrected acceptance suite

**Files:**

- Regenerate `shared/types/database.types.ts`.
- Modify only directly affected type assertions/tests if the generator exposes a real mismatch.

- [ ] **Step 1: Regenerate and inspect Cloud DEV types**

```powershell
pnpm db:dev:target
pnpm db:dev:types
git diff -- shared/types/database.types.ts
```

Expected: the Opportunity row/insert/update types include the five new current invalidation fields; public RPC signatures remain unchanged. Reject unrelated generated schema drift and investigate before continuing.

- [ ] **Step 2: Run focused application tests**

```powershell
pnpm vitest run tests/unit/shared/stage01-schemas.spec.ts tests/unit/server/opportunity-routes.spec.ts tests/unit/server/opportunity.repository.spec.ts tests/unit/server/workflow-routes.spec.ts tests/unit/server/workflow.repository.spec.ts tests/unit/repositories/http-opportunity-repository.spec.ts tests/unit/repositories/http-workflow-repository.spec.ts tests/unit/config/stage01-cloud-dev-concurrency.spec.ts tests/unit/config/supabase-cloud-dev-runner.spec.ts
pnpm typecheck
```

- [ ] **Step 3: Run backend acceptance and Cloud advisors**

```powershell
pnpm db:dev:status
pnpm db:dev:dry-run
pnpm db:dev:stage01:test
pnpm db:dev:stage01:concurrency
pnpm db:dev:rls-smoke
pnpm db:dev:canonical-check
pnpm db:dev:advisors:security
pnpm db:dev:advisors:performance
```

Expected: no pending migration, all Stage 01 tests/races pass, RLS/canonical smoke passes, and advisors return no error-level finding introduced by this correction.

- [ ] **Step 4: Run full application regression**

```powershell
pnpm test:unit
pnpm typecheck
pnpm lint
pnpm build
pnpm test:e2e
```

Report baseline failures separately; never label an unrun or failing command as passed.

- [ ] **Step 5: Run explicit boundary scans**

```powershell
rg -n "service_role|SUPABASE_SERVICE_ROLE" app server shared
rg -n "BDG-(TAX|EVAL|AUTH|HIER)-01|Project Manager|Stage 02|stage02" supabase/migrations shared server app
git diff --name-status 78bf5151f3c46527f53350edd18a79f7f1778677..HEAD
git diff --check 78bf5151f3c46527f53350edd18a79f7f1778677..HEAD
git status --short
```

Interpret matches; the scan is not a pass merely because text exists in tests/docs. Confirm no Stage 01 UI, Project creation, authority resolution, production dependency, or normal-path `service_role` was introduced.

- [ ] **Step 6: Commit generated types or final direct test refinements**

If tracked changes remain:

```powershell
git add shared/types/database.types.ts tests
git diff --cached --check
git commit -m "test: verify stage 01 corrective contracts"
```

Do not create an empty commit.

---

### Task 6: Self-review, deliver the same branch, and report immutable evidence

- [ ] **Step 1: Map fresh evidence to reopened acceptance criteria**

Record:

- `AC-S01-05`: explicit schemaVersion 1 baseline plus later Contact Method mutation proof.
- `AC-S01-08` and `AC-S01-10`: bound-snapshot taxonomy rejection and fail-closed conditional Referrer behavior.
- `AC-S01-09`: all nine real one-winner public-RPC races, exact loser error, final state, and cleanup.
- `AC-S01-14`: shared/HTTP/DB required revalidation evidence.
- duplicate restore finding: stored invalidation semantic plus conditional non-empty separation/correction evidence.
- `AC-S01-19`, `AC-S01-20`, and `AC-S01-21`: generated types, full regression/boundary audit, and remote SHA equality.

- [ ] **Step 2: Review the complete correction delta**

```powershell
git log --oneline --decorate 78bf5151f3c46527f53350edd18a79f7f1778677..HEAD
git diff --stat 78bf5151f3c46527f53350edd18a79f7f1778677..HEAD
git diff --check 78bf5151f3c46527f53350edd18a79f7f1778677..HEAD
git status --short --branch
```

Expected: clean checkout, bounded files, focused commits, and no unrelated diff.

- [ ] **Step 3: Push without merge or force and verify remote equality**

```powershell
git push origin feat/vqh-stage-01-foundation
git rev-parse HEAD
git ls-remote origin refs/heads/feat/vqh-stage-01-foundation
```

Set `head_sha` and `remote_head_sha` from command output. `COMPLETE` requires equality. A failed push after local completion is `LOCAL_COMPLETE_PUSH_BLOCKED`.

- [ ] **Step 4: Return the repository-standard Completion Report**

Include status; `analysis_base_sha`; `remote_base_sha`; `execution_base_sha`; reviewed implementation SHA; `head_sha`; `remote_head_sha`; changed files/commits; acceptance-criteria evidence; every validation command and result; Cloud DEV migration/fixture side effects; cleanup result/residue; production/deployment/PR/merge/force-push statements; risks; review focus; and CI observation. Do not claim CI passed unless a run for `head_sha` is observed.

## Plan Self-Review Checklist

- [x] Every approved design section 5–9 maps to an implementation task and a focused test.
- [x] All twelve taxonomy fields map to the bound immutable snapshot and no business catalog is invented.
- [x] The baseline payload includes every Section 7 evidence group, minimal PII, deterministic ordering, one completion timestamp, event-first atomicity, and SHA-256 proof.
- [x] Revalidation evidence is non-empty across shared schema, HTTP, database event, and audit.
- [x] Duplicate restore behavior distinguishes ordinary invalidation from stored `duplicate_merged` semantics and clears state atomically.
- [x] The concurrency matrix contains all nine approved scenarios, real public RPCs, exact `VERSION_CONFLICT`, final-state proof, and cleanup precedence.
- [x] Migration, Cloud DEV, production, Git, UI, Project, Stage 02, open BDG, authority, and `service_role` boundaries are explicit.
- [x] All interfaces, commands, and commit boundaries are concrete; the sole execution-assigned filename component is the timestamp produced by the required migration generator, and there are no operator-selected paths.
